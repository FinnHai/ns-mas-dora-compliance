# Kanonische Evaluationsdaten (JSON)

Maschinenlesbare Ergebnisse der 80-Run-Evaluation (S1–S4) und der Vergleichsläufe.

| Datei | Inhalt |
|-------|--------|
| `eval_n10_results.json` | S1–S3, je 10× NS-MAS + 10× Baseline (60 Runs aggregiert) |
| `eval_n10_results_backup_20260323_1027.json` | S4 (20 Runs), separat archiviert (fester Dateiname) |
| `comparison/eval_comparison.json` | Illustrationsläufe inkl. `nsmas_s2_r1` (Walkthrough-Daten) |

**Hinweis:** Vollständiger Quellcode, Frontend und Repro-Skripte liegen im **vollständigen Repository**; dieses Dokument beschreibt nur die referenzierten JSON-Dateien. Zusätzliche Rohdaten (Logs, qualitative Läufe) können unter `backend/evaluation/entwicklung_archiv/` liegen (nicht im optionalen ZIP).
